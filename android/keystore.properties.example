# Copy to keystore.properties (gitignored) and fill in local secrets.
# Path is relative to orderbhojan/android/ (the Gradle project root).
storeFile=release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=orderbhojan
keyPassword=YOUR_KEY_PASSWORD
