/** Domain projection module version (M6 PR-4). */
export const PROJECTION_DOMAIN_VERSION = '0.1.0-foundation' as const;

export const DEFAULT_PROJECTION_BATCH_SIZE = 100 as const;
export const DEFAULT_PROJECTION_CONSUMER_GROUP = 'projection-workers' as const;
