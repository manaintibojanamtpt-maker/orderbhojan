# Pricing Read Adapter Domain (M8 PR-11)

Pure domain for **standalone pricing read adapter routing** between legacy repository and shadow projection.

## Read sources

`legacy` · `projection`

**STOP.** Await ARB approval before M8 PR-12 (Controlled Pricing Projection Rollout).
