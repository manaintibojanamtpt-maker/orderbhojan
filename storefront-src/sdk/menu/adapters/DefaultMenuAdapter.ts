/**
 * MenuSDK — default adapter re-export (M7 PR-4).
 * Legacy import path preserved — implementation lives in orchestration/.
 */

export {
  DefaultMenuAdapter,
  createDefaultMenuAdapter,
  type DefaultMenuAdapterDeps,
} from '../orchestration/DefaultMenuAdapter';
