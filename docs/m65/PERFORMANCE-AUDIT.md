# M6.5 Performance Audit

**Status:** Pass

- LazyMotion + domAnimation feature bundle for Framer Motion
- Menu route remains lazy-loaded (M6)
- CSS-only parallax via `animation-timeline` (progressive enhancement)
- Blur-up images avoid layout shift
- No new API calls or state persistence

Gate performance smoke and Lighthouse readiness pass unchanged budgets.
